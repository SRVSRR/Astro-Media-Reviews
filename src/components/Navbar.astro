<nav class="sticky top-0 z-40 backdrop-blur bg-secondary/80 border-b border-soft/60">
  <div class="max-w-screen-xl mx-auto px-4 py-3 flex flex-col gap-3 md:flex-row md:items-center md:justify-between">
    <div class="flex items-center justify-between gap-3">
      <a href="/" class="group flex items-center gap-3 rounded-md transition-all duration-200 active:scale-95 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent/50">
        <span class="text-2xl font-semibold tracking-tight text-white">
          <span class="text-accent group-hover:text-white transition-colors duration-200">SRV</span><span class="group-hover:text-accent transition-colors duration-200">SRR</span>
        </span>
      </a>
      <button id="nav-toggle" class="md:hidden inline-flex items-center justify-center rounded-lg border border-soft px-3 py-2 text-white hover:border-accent focus:outline-none focus:ring-2 focus:ring-accent/50" aria-expanded="false" aria-controls="nav-menu" aria-label="Toggle navigation">
        <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="3" y1="6" x2="21" y2="6"></line>
          <line x1="3" y1="12" x2="21" y2="12"></line>
          <line x1="3" y1="18" x2="21" y2="18"></line>
        </svg>
      </button>
    </div>

    <div id="nav-menu" class="hidden md:block w-full md:w-auto">
      <ul class="flex flex-col md:flex-row md:items-center md:gap-6 text-sm font-medium text-white border border-soft/60 md:border-0 rounded-xl md:rounded-none p-3 md:p-0 bg-secondary/80 md:bg-transparent">
        <li><a href="/" class="block py-2 md:py-0 hover:text-accent transition-colors">Home</a></li>
        <li><a href="/articles" class="block py-2 md:py-0 hover:text-accent transition-colors">All Articles</a></li>
        <li><a href="/skills" class="block py-2 md:py-0 hover:text-accent transition-colors">Skills</a></li>
        <li><a href="/about" class="block py-2 md:py-0 hover:text-accent transition-colors">About</a></li>
      </ul>
    </div>
  </div>
</nav>

<script is:inline>
  document.addEventListener('DOMContentLoaded', () => {
    const toggle = document.getElementById('nav-toggle');
    const menu = document.getElementById('nav-menu');
    if (!toggle || !menu) return;

    toggle.addEventListener('click', () => {
      const expanded = toggle.getAttribute('aria-expanded') === 'true';
      const nowOpen = !expanded;
      toggle.setAttribute('aria-expanded', String(nowOpen));
      menu.classList.toggle('hidden', !nowOpen);
    });
  });
</script>
